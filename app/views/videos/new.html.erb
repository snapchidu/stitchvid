<% if @video.errors.any? %>
 <section id="errors" >
   <h2> <%= pluralize(@video.errors.count, "error") %> prohibited this restaurant from being saved: </h2>
   <ul>
     <% @video.errors.full_messages.each do |message| %>
       <li><%= message %></li>
     <% end %>
   </ul>
 </section>
<% end %>


<div class="container">

    <h1>Add Stitch</h1>

      <%= form_for @video do |f| %>

        <div class="form-group">
          <%= f.label :link %>
          <%= f.text_field :link, class: 'form-control' %>
        </div>

        <div class='form-group'>
          <%= f.label :all_tags %>
          <%= f.text_field :all_tags, class: 'form-control' %>
        </div>

      <%= f.submit 'Upload', class: 'btn btn-primary' %>

      <% end %>

  </div>
